/* eslint-disable */
import { Map } from 'immutable';

export const STATE_SET_VISIBLE_TAB = 'horizontal menu: state show horizontal menu tab';

const initialState = Map({
    currentTab: 0,
});

export default initialState;
